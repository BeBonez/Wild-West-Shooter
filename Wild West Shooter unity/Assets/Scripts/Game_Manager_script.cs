using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game_Manager_script : MonoBehaviour
{
    public int gold = 100;
    // Make this script handle game States (which Scene && currency)
    // Make another script to the manager for UI
    // Make another script to handle game Events = Spawn

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
